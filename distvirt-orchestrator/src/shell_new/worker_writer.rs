//! Per-connection wire encoder for the new async shell.
//!
//! Identical to `task/worker_writer.rs` — re-used here so shell_new/
//! is self-contained.

pub(crate) use crate::task::worker_writer::run;
