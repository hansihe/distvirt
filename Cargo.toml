[workspace]
resolver = "3"
members = ["distvirt-worker", "distvirt-worker-protocol", "distvirt-guest-protocol", "distvirt-cli", "distvirt-compose", "guest-image/guest-init"]

[profile.guest]
inherits = "release"
panic = "abort"
lto = true
strip = true
opt-level = "s"

