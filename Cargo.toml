[workspace]
resolver = "3"
members = ["distvirt-worker", "distvirt-guest-protocol", "distvirt", "distvirt-cli", "distvirt-compose", "guest-image/guest-init"]

[profile.guest]
inherits = "release"
panic = "abort"
lto = true
strip = true
opt-level = "s"

